# Backlog — TROQ

Backlog de **alto nível** por fase. Este documento **não** é o backlog técnico da aplicação: ele registra os trabalhos delimitados de cada fase, com objetivo, dependências e estado ([roadmap.md](roadmap.md)).

**A Fase 0 está concluída desde 2026-09-14.** Todos os itens `F0-xxx` estão `concluído`; o último foi F0-023, que verificou o gate de saída em [phase-1-transition.md](phase-1-transition.md). Essa parte do documento passa a ser registro histórico da fase.

**A Fase 1 está em andamento.** A sua numeração `F1-xxx` foi aberta por F1-001, o primeiro trabalho da fase, conforme previsto pela seção "Fase 1" deste documento. O gate de saída da Fase 1 continua integralmente por satisfazer.

Fontes: [roadmap.md](roadmap.md), [../decisions/open-decisions.md](../decisions/open-decisions.md), [../decisions/decision-log.md](../decisions/decision-log.md), [../product/requirements.md](../product/requirements.md), [risks.md](risks.md).

## Convenções

- IDs por fase — `F0-xxx` na Fase 0, `F1-xxx` na Fase 1 —, sequenciais dentro da fase e nunca reutilizados.
- **Estados:** `concluído`, `próximo` (o próximo trabalho a executar), `pendente` (pode ser executado assim que houver capacidade, sem bloqueio) e `bloqueado` (depende de outro item ainda não concluído).
- Cada item resulta em documento versionado, em código versionado ou em decisão registrada em [../decisions/decision-log.md](../decisions/decision-log.md) e, quando houver, no fechamento da OD correspondente em [../decisions/open-decisions.md](../decisions/open-decisions.md).
- Ordem de execução segue a hierarquia de dependências; itens `pendente` sem dependência entre si podem correr em paralelo.

## Concluído

| ID | Título | Objetivo | Dependências | Estado |
| --- | --- | --- | --- | --- |
| F0-001 | Inspeção inicial do repositório | Registrar o estado factual do repositório antes de qualquer alteração | — | concluído |
| F0-002 | Baseline documental inicial | README, índice, estado do projeto, escopo do MVP, RB-001 a RB-006, OD-01 a OD-11, R-01 a R-10 (PR #1, squash em `main`) | F0-001 | concluído |
| F0-003 | ADRs 0001 a 0003 | Formalizar monólito modular/Next.js, PostgreSQL/Neon e Cloudflare R2 | F0-001 | concluído |

## Trabalho desta entrega (estruturação do baseline)

| ID | Título | Objetivo | Dependências | Estado |
| --- | --- | --- | --- | --- |
| F0-004 | Correção do público-alvo | Registrar o perfil completo do público-alvo em [../product/mvp-scope.md](../product/mvp-scope.md), separando perfil de papéis e preservando OD-11 | F0-002 | concluído |
| F0-005 | Requisitos rastreáveis | Catálogo RF/RNF com rastreabilidade para RB e OD em [../product/requirements.md](../product/requirements.md) | F0-002, F0-003 | concluído |
| F0-006 | Decision log | Registro de decisões vigentes em [../decisions/decision-log.md](../decisions/decision-log.md) | F0-002, F0-003 | concluído |
| F0-007 | Roadmap macro | Fases, entregáveis, dependências e gates em [roadmap.md](roadmap.md) | F0-005 | concluído |
| F0-008 | Workflow de agentes | Governança entre Bruno, ChatGPT, Claude Code e Antigravity em [../engineering/ai-agent-workflow.md](../engineering/ai-agent-workflow.md) | — | concluído |
| F0-009 | Backlog da Fase 0 e índice documental | Este documento e atualização de [../README.md](../README.md) | F0-004 a F0-008 | concluído |

## Próximos trabalhos

Ordem lógica. F0-010 foi **concluído** na sexta execução do spike, em 2026-09-14, com dez dos dez critérios comprovados (ver abaixo), o que desbloqueou F0-011. F0-013 foi concluído e fechou OD-04, o que desbloqueou F0-014, F0-015 e F0-018. F0-014 foi concluído e fechou OD-05 em [../product/image-policy.md](../product/image-policy.md) (DEC-028). F0-015 foi concluído e fechou OD-01 em [../product/negotiation-lifecycle.md](../product/negotiation-lifecycle.md) (DEC-029), o que desbloqueou F0-016. F0-016 foi concluído e fechou OD-02 em [../product/ratings.md](../product/ratings.md) (DEC-030), com RF-017 passando a `definido`. F0-017 foi concluído e fechou OD-03 em [../product/prohibited-items.md](../product/prohibited-items.md) (DEC-031), com RF-018, RF-019 e RF-020 passando a `definido`. F0-018, F0-020, F0-021 e F0-024 foram concluídos na mesma entrega e fecharam, respectivamente, OD-06 em [../product/reselection-policy.md](../product/reselection-policy.md) (DEC-032), OD-10 em [../product/data-retention-policy.md](../product/data-retention-policy.md) (DEC-033), OD-11 em [../product/age-eligibility.md](../product/age-eligibility.md) (DEC-034) e OD-12 em [../product/interest-flow.md](../product/interest-flow.md) (DEC-035); com isso RF-001, RF-006, RF-008, RF-013 e RF-023 passaram a `definido`, assim como RNF-009 e RNF-011.

F0-011 foi **concluído** em 2026-09-14 e fechou OD-08 com [ADR-0004](../adr/0004-mercado-pago-pix.md) (DEC-036): o Mercado Pago deixou de ser candidato e passou a ser o **gateway Pix inicial homologado** do MVP, por Checkout Transparente via Orders API. **F0-019 foi concluído no mesmo dia** e fechou **OD-07**, a última decisão aberta da Fase 0, com [../product/payment-exceptions.md](../product/payment-exceptions.md) (DEC-037). **F0-022 foi concluído em seguida, na mesma data**, e produziu a baseline arquitetural da implementação, mais [ADR-0006](../adr/0006-async-work-scheduling-concurrency.md) (DEC-038). **F0-023 foi concluído por último, ainda em 2026-09-14**, e verificou formalmente o gate de saída da Fase 0 em [phase-1-transition.md](phase-1-transition.md). **Não há mais decisão aberta nem item pendente na Fase 0:** todos os itens `F0-xxx` estão `concluído` e a Fase 0 está encerrada.

| ID | Título | Objetivo | Dependências | Estado |
| --- | --- | --- | --- | --- |
| F0-010 | Spike do gateway Pix para exatamente R$ 0,99 | Provar, em sandbox do primeiro candidato (Mercado Pago) e, se necessário, de alternativas, a cobrança de exatamente R$ 0,99, confirmação, webhook, idempotência e tarifas (DEC-018, R-01). O spike é descartável e não entra no código do produto | F0-005 | concluído (10 de 10 critérios comprovados na 6ª execução, 2026-09-14) |
| F0-011 | Registrar resultado e decisão do gateway | Fechar OD-08 com [ADR-0004](../adr/0004-mercado-pago-pix.md): Mercado Pago homologado como gateway Pix inicial do MVP, por Checkout Transparente via Orders API, com idempotência, validação de assinatura por manifesto único, webhook configurado na aplicação e processamento idempotente e reconciliável (DEC-036); atualizar decision log, riscos R-01 e R-04 e requisitos RF-011 e RF-012 | F0-010 | concluído |
| F0-012 | Fechar ORM e estratégia de migrations | Fechar OD-09 com [ADR-0005](../adr/0005-prisma-orm-migrations.md): Prisma ORM 7.x, Prisma Migrate, política dev/staging/produção, `db push` e migrations destrutivas (DEC-026) | F0-003 | concluído |
| F0-013 | Detalhar ciclo de vida do anúncio | Fechar OD-04 em [../product/listing-lifecycle.md](../product/listing-lifecycle.md): estados, transições, visibilidade pública e efeitos sobre interesses e solicitações (DEC-027) | F0-005 | concluído |
| F0-014 | Definir regras de imagens | Fechar OD-05 em [../product/image-policy.md](../product/image-policy.md): quantidade, formatos, tamanho, validação, processamento, derivados e visibilidade das imagens (DEC-028); atualizar RF-004, RF-006, RF-020 e RNF-005 | F0-013 | concluído |
| F0-015 | Definir mecanismo de encerramento | Fechar OD-01 em [../product/negotiation-lifecycle.md](../product/negotiation-lifecycle.md): estados `active` e `closed`, encerramento unilateral por qualquer uma das partes, confirmação do próprio ator, irreversibilidade, ausência de prazos e automação, idempotência e auditoria (DEC-029); atualizar RF-016, RF-017 e RF-022 | F0-013 | concluído |
| F0-016 | Definir avaliações | Fechar OD-02 em [../product/ratings.md](../product/ratings.md): natureza bilateral sobre a contraparte, elegibilidade a partir de negociação `closed`, nota inteira de 1 a 5 sem texto livre, janela de 14 dias corridos, publicação cega, edição antes da publicação, imutabilidade após a publicação, média simples com contagem e invalidação administrativa auditada (DEC-030); atualizar RF-017 e RF-022 | F0-015 | concluído |
| F0-017 | Definir catálogo/política de itens proibidos | Fechar OD-03 em [../product/prohibited-items.md](../product/prohibited-items.md): catálogo por categorias PI-01 a PI-12 com fundamento `ilegal`, `regulado` ou `política`, ausência de fluxo de autorização documental no MVP, regra de casos ambíguos, declaração de conformidade na publicação, fluxo de denúncia, fluxo de moderação, critérios e efeitos da remoção, prazos de 24 horas corridas na classe crítica e 5 dias úteis na comum, reincidência, contestação, auditoria e privacidade do denunciante (DEC-031); atualizar RF-004, RF-018 a RF-020 e RF-022 | F0-005 | concluído |
| F0-018 | Definir política de desistência e reseleção | Fechar OD-06 em [../product/reselection-policy.md](../product/reselection-policy.md): reseleção permitida sob cinco pré-condições simultâneas (escolha anterior existente, negociação anterior `closed`, anúncio `published`, candidato com pagamento aprovado e candidato ainda não selecionado), confirmação explícita do anunciante, imutabilidade da escolha e da liberação anteriores, nova negociação e nova autorização auditada, exclusividade da negociação `active`, preservação literal de RB-003 e tratamento da desistência sem reembolso ou automação (DEC-032); atualizar RF-013 e RF-015 | F0-013 | concluído |
| F0-019 | Definir tratamento de exceções de pagamento | Fechar OD-07 em [../product/payment-exceptions.md](../product/payment-exceptions.md): fonte de verdade do pagamento, identidade e idempotência da tentativa, duplicidade técnica, instante de acreditação como critério de tempestividade, piso de 30 minutos da janela de reserva, preservação de RB-003 sob concorrência, falhas de confirmação e de webhook, quatro hipóteses exaustivas de reembolso técnico, distinção entre reembolso técnico, devolução Pix, MED e chargeback de cartão, efeitos das reversões, reconciliação autoritativa, auditoria e segurança (DEC-037); atualizar RF-009 a RF-012, RF-020 e RF-022 | F0-011 | concluído |
| F0-020 | Definir retenção e exclusão de dados | Fechar OD-10 em [../product/data-retention-policy.md](../product/data-retention-policy.md): minimização por categoria, exclusão de conta com efeito imediato e prazo de 30 dias, expurgo de imagens, 6 meses de log de acesso, 24 meses de auditoria e de registros de moderação e abuso, 5 anos de metadados financeiros, backups até o ciclo normal com máximo de 30 dias adicionais e legal hold registrado (DEC-033); atualizar RF-006, RF-020, RF-022, RF-023, RNF-009 e RNF-011 | F0-005 | concluído |
| F0-021 | Definir elegibilidade etária formal | Fechar OD-11 em [../product/age-eligibility.md](../product/age-eligibility.md): 18 anos completos ou mais como decisão conservadora de escopo do produto, declaração explícita registrada no cadastro, ausência de coleta documental ou biométrica e de serviço externo de verificação, e bloqueio cautelar diante de evidência razoável de menoridade (DEC-034); atualizar RF-001 e RNF-009. O público-alvo de 18 a 50 anos não determinou esta decisão | F0-005 | concluído |
| F0-022 | Produzir arquitetura de dados e API pré-implementação | [../architecture/overview.md](../architecture/overview.md), [../architecture/data-model.md](../architecture/data-model.md), [../architecture/payments-design.md](../architecture/payments-design.md) e [../architecture/contact-release.md](../architecture/contact-release.md), com base nas decisões fechadas; produziu também [ADR-0006](../adr/0006-async-work-scheduling-concurrency.md) (DEC-038) | F0-011, F0-012, F0-013, F0-014, F0-015, F0-018, F0-019, F0-020, F0-024 | concluído |
| F0-023 | Preparar transição para a Fase 1 | Verificar o gate de saída da Fase 0 em [roadmap.md](roadmap.md), revisar riscos e produzir o prompt inicial da Fase 1 | F0-022 | concluído |
| F0-024 | Definir natureza da demonstração de interesse | Fechar OD-12 em [../product/interest-flow.md](../product/interest-flow.md): ação gratuita de interface, sem entidade persistida, sem ocupar vaga paga, sem cancelamento e sem visibilidade ao anunciante, admitida apenas telemetria agregada de funil (DEC-035); atualizar RF-008 | F0-005 | concluído |

### Histórico e conclusão de F0-010

As seis execuções de F0-010 estão registradas em [spikes/f0-010-mercado-pago-pix-r099.md](spikes/f0-010-mercado-pago-pix-r099.md). As cinco primeiras foram classificadas como `INCONCLUSIVO`, por causas sucessivamente menores; a sexta é `CONCLUSIVO` e fechou o item.

As duas primeiras, em 2026-09-07 e 2026-09-14, não executaram nenhum experimento autenticado, por ausência de credencial de teste do Mercado Pago.

A terceira, em 2026-09-14, foi a **primeira com Access Token de teste** e executou a parte experimental. **Oito dos dez critérios passaram a comprovados:** API autenticada, criação da cobrança Pix, aceitação de exatamente R$ 0,99, geração de QR Code e copia e cola com CRC conferido, transição de status até `processed/accredited`, idempotência por `X-Idempotency-Key`, tarifa efetiva e ausência de incompatibilidade com RB-004. Em particular, a tarifa sobre R$ 0,99 deixou de ser indeterminada: no ambiente de teste ela é de R$ 0,01, com líquido de R$ 0,98, por arredondamento meio-para-cima ao centavo e sem tarifa mínima.

A quarta, também em 2026-09-14, atacou apenas os dois critérios de webhook que restavam e **fechou um deles**. Um endpoint HTTPS público foi provisionado em projeto Vercel temporário e isolado, sem qualquer relação com o projeto oficial `techlab-troq`, e o Mercado Pago **entregou notificações reais a esse endpoint**, com `x-signature`, `x-request-id`, `ts` e `data.id` presentes, correlacionadas à `merchant_order` `44469080694` de R$ 0,99. O critério 7 passou a comprovado. Nessa execução também se descobriu que a URL de notificação é configurável por requisição via `notification_url` de `POST /checkout/preferences`. **Isso vale para a API de Preferences e não para a de Orders:** a sexta execução confirmou que `POST /v1/orders` **rejeita** `notification_url` com `HTTP 400` e `unsupported_properties`, e por isso [ADR-0004](../adr/0004-mercado-pago-pix.md) proíbe enviá-la nesse endpoint.

Restava **um único critério**: validar a assinatura de uma notificação real. Ele dependia de um único insumo, a **chave secreta de webhook**, que nenhum endpoint público da API expõe ou permite configurar e que só existe no painel “Suas integrações”. A quinta execução, também em 2026-09-14, refinou esse diagnóstico: o painel é **legível** com a sessão de navegador do próprio Bruno; o que bloqueava era a **escrita**, sujeita a reautenticação por **TOTP**.

A sexta execução, ainda em 2026-09-14, **fechou o critério 8 e concluiu o item**. Com a aplicação `TROQ F0-010 Seller Test Webhook` (`application_id` `4982497380871264`) criada sob um Seller Test User, o simulador oficial produziu HMAC válido contra o endpoint temporário, e uma **Order Pix nova de exatamente R$ 0,99** gerou uma **notificação real** do Mercado Pago, com `application_id` correspondente à mesma aplicação, correlacionada por `data.id` e por `external_reference`, cujo HMAC-SHA256 conferiu contra o manifesto oficial; o receiver devolveu `HTTP 200`, e a matriz negativa (assinatura, `data.id`, `x-request-id` e `ts` adulterados) devolveu `HTTP 401` em todos os casos. Os recursos temporários foram removidos em seguida, incluindo a aplicação de teste e o projeto Vercel.

Com dez de dez critérios comprovados, F0-010 ficou `concluído` e a validação técnica deixou de ser o bloqueio da Fase 0. O spike, por natureza, provou viabilidade técnica sem homologar gateway; a homologação veio em seguida, em F0-011. Todas as decisões de produto que não dependiam de pagamento já haviam sido fechadas (OD-01 a OD-06 e OD-09 a OD-12).

### Conclusão de F0-011

F0-011 foi executado em **2026-09-14**, imediatamente após F0-010, e produziu [ADR-0004](../adr/0004-mercado-pago-pix.md), que **fecha OD-08** e registra DEC-036. A decisão: o **Mercado Pago é o gateway Pix inicial homologado** do MVP, integrado por Checkout Transparente tendo a **Orders API** como superfície principal. A ADR também fixa restrições normativas que vieram diretamente das evidências do spike — `X-Idempotency-Key` na criação, validação de autenticidade antes de qualquer processamento, **uma única** regra oficial de manifesto HMAC sem fallback entre variantes, proibição de `notification_url` no corpo de `POST /v1/orders`, processamento idempotente e reconciliável por `GET /v1/orders/{id}` e isolamento do vocabulário do provedor em um módulo adaptador.

O que F0-011 **não** fez: nenhuma linha de integração de pagamento, nenhum SDK, nenhum schema, nenhuma migration, nenhum secret e nenhuma configuração no painel do Mercado Pago. A tarifa de R$ 0,01 sobre R$ 0,99 observada em sandbox permanece registrada como evidência de ambiente de teste, **não** como tarifa contratada de produção — a confirmação da tarifa real é pré-requisito do lançamento comercial e está registrada como risco residual em R-01.

Com OD-08 fechada, **OD-07 passou a ser a única decisão aberta da Fase 0** e **F0-019** passou a `próximo`.

### Conclusão de F0-019

F0-019 foi executado em **2026-09-14**, logo depois de F0-011, e produziu [../product/payment-exceptions.md](../product/payment-exceptions.md), que **fecha OD-07** e registra DEC-037. A política é normativa e implementável: o estado autoritativo do pagamento é o que o Mercado Pago reporta em consulta direta à order, e a notificação é apenas gatilho, nunca fonte de estado; somente pagamento efetivamente acreditado produz efeito de negócio e estado incerto nunca é resolvido a favor da aprovação; a tentativa de pagamento tem identidade estável por reserva, e criação, notificação, transições, reembolso e cancelamento são idempotentes; duplicidade técnica elege um pagamento canônico, nunca consome duas vagas e devolve o excedente; o instante decisivo da tempestividade é o de acreditação, de modo que confirmação atrasada de pagamento feito a tempo vale e pagamento acreditado fora da janela não cria vaga nem solicitação; a janela de reserva tem piso de 30 minutos, alinhado ao mínimo aceito pela Orders API; nenhuma exceção ultrapassa três solicitações pagas válidas; o reembolso técnico é integral e tem quatro hipóteses exaustivas; `chargeback` é vocabulário de cartão e não descreve o Pix, e desacordo comercial não é hipótese de MED; reversão externa preserva o histórico, torna inelegível a solicitação ainda não escolhida, não revoga contato já liberado e não devolve a vaga.

A pesquisa externa que sustenta a decisão foi feita em 2026-09-14 contra a documentação do Mercado Pago Developers e o *Guia de implementação dos procedimentos de devolução no Pix, com ênfase no Mecanismo Especial de Devolução* (versão 4.3) do Banco Central, e está registrada na seção 4 daquele documento.

O que F0-019 **não** fez: nenhuma linha de integração, nenhum endpoint, nenhum receiver de webhook, nenhum schema, nenhuma migration, nenhum secret, nenhum acesso a painel produtivo e nenhum pagamento real. Com OD-07 fechada, **não resta decisão aberta na Fase 0** e **F0-022** passou a `próximo`.

### Conclusão de F0-022

F0-022 foi executado em **2026-09-14**, logo depois de F0-019, e produziu os quatro documentos previstos:

- [../architecture/overview.md](../architecture/overview.md) — objetivos arquiteturais em ordem de prioridade, quatro camadas com dependência unidirecional, os **nove módulos de domínio** do MVP, quatro fronteiras de confiança, autorização em três níveis cumulativos verificados na mesma transação do efeito, classificação dos dados pessoais em três faixas, trilha de auditoria única e append-only, observabilidade mínima e política de evolução sem microserviços prematuros.
- [../architecture/data-model.md](../architecture/data-model.md) — entidades, relações, cardinalidades, estados, unicidades e um quadro de **14 invariantes críticas** com a indicação de onde cada uma é protegida: restrição de banco, transação ou aplicação.
- [../architecture/payments-design.md](../architecture/payments-design.md) — a política de DEC-037 convertida em mecanismo, com **CI-1 a CI-12 rastreados um a um** e um contrato de teste de 18 casos.
- [../architecture/contact-release.md](../architecture/contact-release.md) — RB-001 convertida em mecanismo, com a separação explícita entre **armazenar, autorizar e retornar** o contato.

As decisões de desenho mais relevantes: a proteção de RB-003 passa a ser **índice único parcial** sobre a vaga do anúncio, com a expiração resolvida na própria transação que aloca — de modo que a invariante não dependa de nenhum trabalho periódico; a janela de reserva é de **exatamente 30 minutos**, o único valor em que o `expiration_time` da order pode coincidir com o fim da janela sem a cobrança sobreviver à reserva; a chave de idempotência é **persistida**, nunca rederivada; a idempotência de negócio é feita no **efeito**, não na deduplicação da entrega da notificação; o pagamento canônico é eleito por menor instante de acreditação, com desempate determinístico; e o contato vive em tabela própria sob um módulo com ponto de entrada autorizado, de modo que a consulta descuidada não o alcance.

F0-022 exigiu **uma** decisão arquitetural nova e duradoura, registrada em [ADR-0006](../adr/0006-async-work-scheduling-concurrency.md) (DEC-038): trabalho assíncrono, agendamento e concorrência. A pesquisa externa que a sustenta apurou que a entrega do agendamento da Vercel é *best effort*, que falhas não são retentadas, que execuções podem se sobrepor, que o plano Hobby limita o agendamento a uma execução diária e que o endpoint pooled do Neon **não** suporta trava consultiva de sessão. O primeiro conjunto de fatos transformou "nenhuma invariante depende de um job" de boa prática em requisito; o último tornou incorreta — e não apenas subótima — a trava de sessão. Nenhuma tecnologia nova foi adotada.

O que F0-022 **não** fez: nenhuma linha de código de produto, nenhum endpoint, nenhum componente, nenhum schema Prisma, nenhuma migration, nenhum secret, nenhuma integração real com o Mercado Pago, nenhum provisionamento e nenhum deploy. Com F0-022 concluído, **F0-023** passou a `próximo` e foi executado em seguida.

### Conclusão de F0-023

F0-023 foi executado em **2026-09-14**, logo depois de F0-022, e é o **último item da Fase 0**. Ele produziu [phase-1-transition.md](phase-1-transition.md), que audita o gate de saída da Fase 0 em matriz explícita de critério → fonte normativa → evidência → resultado. Os dez critérios (G-1 a G-10) resultaram `PASS` e o gate foi declarado **APROVADO**.

O trabalho também verificou individualmente os **12 requisitos** dos quais [roadmap.md](roadmap.md) diz que a Fase 1 e a Fase 2 dependem — 9 em `definido`, 3 em `parcialmente definido` (RF-004, RF-021 e RNF-018), nenhum `bloqueado`, nenhum sem fonte —, revisou os **onze riscos** R-01 a R-11 sem encerrar nenhum e sem alterar texto artificialmente, e classificou factualmente os **sete entregáveis** da Fase 1: `já existente` os padrões de projeto e o CI; `parcial` o scaffold (faltam os módulos de AR-3.3) e a infraestrutura mínima (o projeto Vercel existe; R2 e Resend não); `não iniciado` o contrato de ambientes, o banco/Prisma/schema e a observabilidade.

O item da Fase 1 escolhido como próximo trabalho é **F1-001 — contrato de ambientes e segredos**, cujo prompt executor foi versionado em [prompts/f1-001-environments-and-secrets.md](prompts/f1-001-environments-and-secrets.md). A escolha é deliberada: scaffold, comandos de qualidade e CI já existem e não devem ser recriados, enquanto o contrato de ambientes é o único entregável `não iniciado` que é pré-requisito seguro para conectar Neon, Prisma, R2, Resend, autenticação e pagamentos.

O que F0-023 **não** fez: nenhuma linha de código, nenhuma alteração de regra de negócio, de requisito, de ADR ou de decisão vigente, nenhuma decisão nova, nenhum provisionamento, nenhum segredo e nenhuma implementação da Fase 1. Nenhum item `F0-xxx` novo foi criado para trabalho de Fase 1: a Fase 1 usa a sua própria numeração.

## Fase 1

A Fase 0 está encerrada e a Fase 1 está **em andamento**. A numeração `F1-xxx` foi aberta por **F1-001**, o primeiro trabalho da fase, como esta seção previa.

O estado factual dos sete entregáveis da Fase 1 — o que já existe, o que é parcial e o que não foi iniciado — está em [phase-1-transition.md](phase-1-transition.md), seção 10, como registro da auditoria; o estado corrente está em [../project-state.md](../project-state.md). Dos sete, o contrato de ambientes (E-4) foi **concluído** por F1-001; o banco com Prisma e schema inicial (E-5) foi **concluído** por F1-002 (schema e migration), F1-003 (runtime do Prisma Client) e F1-004 (Neon de `preview`, custódia das conexões, Vercel Preview e workflow de `migrate deploy`, com a migration inicial aplicada); e o scaffold com a estrutura de módulos (E-1) foi **concluído** por F1-005, que materializou os nove módulos de domínio de AR-3.3 e os dois transversais; e a infraestrutura mínima (E-6) foi **concluída** por F1-006 (Cloudflare R2 de `development` e de `preview`) e F1-007 (Resend dos mesmos dois ambientes), somados ao projeto Vercel que já existia. Resta apenas **E-7**, **não iniciado**: F1-008 decidiu o provedor de observabilidade em [../adr/0007-observability-sentry.md](../adr/0007-observability-sentry.md) (DEC-039) e escreveu o contrato das suas variáveis, o que fecha a lacuna de definição de AR-14.1 e de RNF-018 mas **não** provisiona nada; e F1-009 provisionou os dois projetos Sentry isolados de `development` e `preview`, com a privacidade configurada no provedor e as DSNs custodiadas nos ambientes, o que também **não** conclui o entregável — **não** instala SDK e **não** emite telemetria. Decisão documental não é entrega operacional, e infraestrutura provisionada não é observabilidade funcional da aplicação: a instrumentação é F1-010. O scaffold e os comandos de qualidade **não** são recriados, e o CI e o seu job **não** são renomeados.

| ID | Título | Objetivo | Dependências | Estado |
| --- | --- | --- | --- | --- |
| F1-001 | Contrato de ambientes e segredos | Criar [../engineering/environments.md](../engineering/environments.md) como fonte normativa dos três ambientes `development`, `preview` e `production`, com critério objetivo de classificação entre variável pública e exclusivamente server-side, proibição sem exceção de segredo em variável pública, catálogo das variáveis por área — aplicação, PostgreSQL/Neon com endpoint pooled e direto, Cloudflare R2, Resend, Better Auth, Mercado Pago e segredo de agendamento — e regras operacionais de custódia, adição, rotação e vazamento; criar `.env.example` apenas com nomes e placeholders seguros; preservar a proteção de `.env` reais no `.gitignore`. **Sem provisionar serviço externo, sem credencial real e sem iniciar o schema de domínio** | Gate da Fase 0 (F0-023) | concluído |
| F1-002 | Fundação Prisma e schema físico inicial | Instalar `prisma` e `@prisma/client` em **7.10.0** exato; criar `prisma.config.ts` com `DIRECT_URL` para o CLI e `DATABASE_URL` reservada ao runtime; materializar `prisma/schema.prisma` a partir de [../architecture/data-model.md](../architecture/data-model.md) (24 models, 18 enums, sem `Interest`); gerar a migration inicial por `--create-only` e complementá-la com o SQL das invariantes I-1, I-2, I-5, I-6 e I-8 e das demais restrições já definidas (índices únicos parciais, `CHECK`s e triggers, sem Preview feature); provar cada garantia por caso negativo em PostgreSQL real e descartável, com `migrate deploy` do zero e reaplicação após recriação; documentar em [../engineering/database.md](../engineering/database.md) e atualizar o contrato de ambientes. **Sem Neon, sem credencial real, sem runtime do Prisma Client, sem job de `migrate deploy` e sem funcionalidade de produto** | F1-001 | concluído |
| F1-003 | Runtime Prisma Client e fronteira mínima de persistência | Instalar `@prisma/adapter-pg` e `pg` em versão exata compatível com Prisma 7.10.0; criar a fronteira server-side `src/persistence/prisma.ts`, único ponto que instancia o `PrismaClient` com `PrismaPg` sobre `DATABASE_URL` (pooled), nunca `DIRECT_URL`, com inicialização lazy, instância única em `globalThis` e erro controlado sem valor quando a variável falta; testes unitários na suíte padrão e prova de integração (`npm run test:integration`) contra PostgreSQL real e descartável migrado por `migrate deploy`; documentar em [../engineering/database.md](../engineering/database.md), seção 13, e passar `DATABASE_URL` a `consumido` no contrato de ambientes. **Sem Neon, sem credencial real, sem job de `migrate deploy`, sem repositórios de domínio, sem alteração de schema e sem funcionalidade de produto** | F1-002 | concluído |
| F1-004 | Neon de preview e migrações controladas | Provisionar o Neon exclusivo de `preview` (projeto `techlab-troq-preview`, São Paulo, PostgreSQL 17, branch `preview`, database `troq`, sem dados reais); obter do provedor a conexão direta e a pooled e verificar as duas com `SELECT 1`; guardar `DIRECT_URL` e `DATABASE_URL` como secrets do GitHub Environment `preview`, restrito a `main`; configurar `DATABASE_URL` e `APP_ENV=preview` **somente** no escopo Preview da Vercel; criar `.github/workflows/migrate-preview.yml`, que aplica `prisma migrate deploy`, confere `migrate status` e executa `npm run test:integration` somente a partir de `main`, serializado e sem gatilho de PR; confirmar pelo workflow a aplicação da migration inicial ao Neon; documentar em [../engineering/database.md](../engineering/database.md), seção 15, e sincronizar o estado documental. **Sem produção, sem banco por PR, sem alteração de schema, migration, dependências ou ruleset, sem tornar o workflow obrigatório e sem funcionalidade de produto** | F1-003 | concluído |
| F1-005 | Estrutura modular do monólito | Materializar em `src/modules/<module>/index.ts` os **nove módulos de domínio** de [../architecture/overview.md](../architecture/overview.md) (AR-3.3) — `identity`, `contact`, `listing`, `media`, `request`, `payments`, `negotiation`, `reputation`, `moderation` — e os **dois transversais** `audit` e `platform`, cada um com `index.ts` como fronteira pública e sem lógica funcional; registrar a convenção física em [../engineering/conventions.md](../engineering/conventions.md), seção 2.5, incluindo a proibição de barrel global e de import que atravesse o `index.ts`; concluir **E-1**. **Sem funcionalidade de produto, sem entidade, serviço, repositório, DTO ou caso de uso especulativo, sem dependência nova, sem alteração de `schema.prisma`, de migration ou de `src/app`, e sem ADR novo** | F1-004 | concluído |
| F1-006 | Cloudflare R2 de development e preview | Provisionar na conta Cloudflare os dois buckets isolados `troq-media-development` e `troq-media-preview`, ambos em classe `Standard`, localização automática do provedor e **privados** — sem `r2.dev`, sem Custom Domain, sem CORS e sem qualquer exposição pública —, conforme [../adr/0003-object-storage-r2.md](../adr/0003-object-storage-r2.md) e DEC-028; criar **uma Account API Token por ambiente**, com permissão *Object Read & Write* limitada ao bucket daquele ambiente; custodiar as cinco variáveis do contrato em `.env.local` (development, não versionado) e **somente** no escopo Preview da Vercel (preview); provar por API S3-compatible o ciclo `HeadBucket`/`PUT`/`GET`/`DELETE` em cada bucket, sem resíduo, e comprovar o isolamento cruzado entre as credenciais; sincronizar o estado documental. **Sem produção, sem Resend, sem integração no módulo `media`, sem SDK ou dependência nova, sem presigned URL, sem domínio público de imagens e sem ADR novo** | F1-005 | concluído |
| F1-007 | Resend de development e preview | Provisionar o email transacional (DEC-015) verificando no Resend **um subdomínio remetente por ambiente** sob `troqs.app` — `dev.troqs.app` e `preview.troqs.app` —, com os registros DKIM, SPF e MX publicados no DNS da Cloudflare; criar **uma API key por ambiente**, com permissão *Sending access* e restrição ao domínio daquele ambiente; custodiar `RESEND_API_KEY` e `EMAIL_FROM` em `.env.local` (development, não versionado) e **somente** no escopo Preview da Vercel (preview); provar envio real a partir de cada domínio usando exclusivamente o endereço **simulado** `delivered@resend.dev`, nunca pessoa real, e comprovar o isolamento cruzado entre as chaves; concluir **E-6**. **Sem produção, sem integração em código, sem SDK ou dependência nova, sem template de email, sem webhook, sem observabilidade e sem ADR novo** | F1-006 | concluído |
| F1-008 | Decisão arquitetural e contrato da observabilidade | Pesquisar a documentação oficial atual de Sentry, Better Stack e observabilidade nativa da Vercel/Drains e formalizar a escolha em [../adr/0007-observability-sentry.md](../adr/0007-observability-sentry.md) (DEC-039): Sentry SaaS como plataforma de observabilidade do MVP, **um projeto e um DSN por ambiente**, escopo funcional cobrindo erro não tratado de browser e de servidor, logs estruturados, tracing de diagnóstico, associação ao ambiente e à release e os sinais de AR-14.3, **Session Replay fora do MVP**, lista explícita de dados proibidos na telemetria, proibição de telefone/WhatsApp como chave de correlação, preservação do *data scrubbing* do provedor, reafirmação de AR-14.2 (observabilidade não é auditoria) e registro da dependência externa em R-08; registrar em [../engineering/environments.md](../engineering/environments.md), seção 5.8, o contrato das quatro variáveis `NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_AUTH_TOKEN`, `SENTRY_ORG` e `SENTRY_PROJECT`, todas `previsto`, sem criar variável de ambiente ou de release; espelhar em `.env.example`; promover **RNF-018** a `definido`; atualizar AR-14.1 e sincronizar o estado documental. **Sem provisionar Sentry, sem conta, organização ou projeto, sem DSN ou token real, sem instalar `@sentry/nextjs`, sem instrumentation file, sem logger, sem source maps, sem dashboard ou alerta, sem alterar `next.config.*`, `src/` ou `prisma/`, e sem concluir E-7** | F1-007 | concluído |
| F1-009 | Provisionamento do Sentry em development e preview | Criar os projetos Sentry reais **um por ambiente** conforme [../adr/0007-observability-sentry.md](../adr/0007-observability-sentry.md), decisão 2 — `development` e `preview`, sem `production` —; configurar no provedor as opções de privacidade que aquele ADR exige (manter o *data scrubbing* server-side habilitado, acrescentar campos sensíveis do domínio, **não** habilitar `sendDefaultPii` ou sucessora, **não** habilitar Session Replay); obter DSN por ambiente e, se e somente se o upload de source maps for de fato adotado, credencial de build de **menor escopo** por ambiente; custodiar `NEXT_PUBLIC_SENTRY_DSN` e as demais variáveis aplicáveis em `.env.local` (development, não versionado) e **somente** no escopo Preview da Vercel (preview), conforme a seção 6.1 de [../engineering/environments.md](../engineering/environments.md); comprovar o isolamento entre ambientes; sincronizar o estado documental. **Sem `production`, sem instalar `@sentry/nextjs`, sem instrumentar telemetria, sem alterar `next.config.*`, `src/` ou `prisma/`, sem dashboard ou alerta, sem integração Vercel–Sentry e sem declarar E-7 concluído** | F1-008 | concluído |
| F1-010 | Instrumentação do Sentry em development e preview | Instalar `@sentry/nextjs` e integrar a SDK ao App Router conforme [../adr/0007-observability-sentry.md](../adr/0007-observability-sentry.md), decisão 3, lendo a DSN de `NEXT_PUBLIC_SENTRY_DSN` já provisionada por F1-009 e passando o ambiente explicitamente a partir de `APP_ENV` (decisão 11); cobrir erro não tratado de browser e de servidor, logs estruturados e o tracing necessário ao diagnóstico, **sem** habilitar Session Replay e **sem** habilitar `sendDefaultPii` ou a opção que a suceder (decisões 7 e 9); garantir na fronteira que nenhum dado da lista proibida da decisão 5 chegue à telemetria e que telefone/WhatsApp não seja chave de correlação em nenhuma forma derivada (decisão 6); validar a emissão em `development` e em `preview` contra os projetos isolados que já existem; promover a `consumido` apenas a variável que passar a ser efetivamente lida por código versionado, pelo procedimento da seção 6.2 de [../engineering/environments.md](../engineering/environments.md). **Sem `production`, sem source maps, sem release, sem `SENTRY_AUTH_TOKEN`, sem `SENTRY_ORG` ou `SENTRY_PROJECT` como variáveis enquanto source maps não forem adotados, sem dashboard, sem alerta e sem integração Vercel–Sentry** | F1-009 | próximo |

### Conclusão de F1-001

F1-001 foi executado em **2026-09-14**, a partir do prompt versionado em [prompts/f1-001-environments-and-secrets.md](prompts/f1-001-environments-and-secrets.md), e é o primeiro trabalho da Fase 1. Ele produziu [../engineering/environments.md](../engineering/environments.md) e o seu espelho versionado `.env.example`, e marcou `engineering/environments.md` como **existente** no índice de [../README.md](../README.md).

A decisão estruturante do documento é o **critério objetivo de classificação**: a classe de uma variável é determinada por uma única pergunta verificável — o valor precisa ser lido por código que executa no navegador? —, cuja resposta afirmativa exige o prefixo `NEXT_PUBLIC_` e, com ele, o embutimento do valor no bundle de cliente. Daí decorre, sem depender de julgamento, que **variável pública é conteúdo público** e que nenhum segredo pode ocupá-la. O documento também separa `server-side` de `segredo`, para que a classificação não confunda configuração discreta com credencial, e registra que telefone/WhatsApp e qualquer dado pessoal real não são variável de ambiente de classe nenhuma.

Cada variável do catálogo é rastreável a uma fonte normativa: `DATABASE_URL` e `DIRECT_URL` à exigência de conexão pooled e direta de [../adr/0005-prisma-orm-migrations.md](../adr/0005-prisma-orm-migrations.md), decisão 10, reforçada pelo fato de que o endpoint pooled do Neon não suporta recursos de sessão ([../adr/0006-async-work-scheduling-concurrency.md](../adr/0006-async-work-scheduling-concurrency.md)); as variáveis do R2 à API S3-compatible de [../adr/0003-object-storage-r2.md](../adr/0003-object-storage-r2.md) e aos derivados públicos de [../product/image-policy.md](../product/image-policy.md); o Access Token e a chave de webhook à decisão 6 de [../adr/0004-mercado-pago-pix.md](../adr/0004-mercado-pago-pix.md); e `CRON_SECRET` à decisão 9 de ADR-0006. Ficou registrado que a URL de webhook do Mercado Pago **não** é variável de ambiente, por ser configurada no nível da aplicação, e que enviar `notification_url` em `POST /v1/orders` continua proibido.

O que F1-001 **não** fez: nenhum serviço provisionado, criado, configurado ou acessado — Neon, Cloudflare R2, Resend, Mercado Pago e Vercel seguem exatamente como estavam —, nenhuma credencial real usada ou gerada, nenhuma variável definida em painel de provedor, nenhuma dependência instalada, nenhum `prisma/`, `schema.prisma` ou migration, nenhum módulo de domínio, nenhuma funcionalidade de produto, nenhum parser ou validador de variáveis em código, nenhuma alteração no CI ou no ruleset e nenhuma alteração de regra de negócio, requisito, ADR ou decisão vigente. Na data de F1-001, nenhuma variável do catálogo era lida por código; F1-002 mudou isso apenas para `DIRECT_URL`. A Fase 1 **não** foi declarada concluída.

### Conclusão de F1-002

F1-002 foi executado em **2026-09-14**, logo depois de F1-001, e é a **primeira parte do entregável E-5**. Ele instalou `prisma@7.10.0` e `@prisma/client@7.10.0` em versão exata — conferido no registro npm que `prisma@latest` ainda resolve para `8.0.0-rc.15` e que não há 7.x posterior a 7.10.0 —, criou `prisma.config.ts` (generator `prisma-client` com saída em `src/generated/prisma`, não versionada; `DIRECT_URL` para as operações de schema do CLI; `DATABASE_URL` reservada ao runtime futuro), materializou `prisma/schema.prisma` com os 24 models e 18 enums nativos de [../architecture/data-model.md](../architecture/data-model.md), UUID nativo sem extensão, `timestamptz(6)` e dinheiro em centavos inteiros, e produziu a migration inicial `20260914210926_initial_schema`: SQL gerado por `--create-only` mais um bloco customizado com 4 índices únicos parciais, 10 `CHECK`s, 4 funções e 3 triggers. As invariantes **I-1** (índice único parcial de `(listing_id, slot_index)` sobre `reserved`/`paid` mais `slot_index` em 1..3), **I-2** (tentativa única por solicitação), **I-5** (negociação `active` única por anúncio), **I-6** (escolha única por solicitação) e **I-8** (trigger que impede qualquer saída de `paid`) passaram a ser garantias reais de banco, junto com a matriz T1..T9 do anúncio, a faixa 1..6 das imagens, a nota 1..5, a unicidade de denúncia, contestação e liberação e a unicidade de email entre contas não excluídas. Cada garantia foi provada por caso negativo em PostgreSQL 17 real em contêiner descartável; `migrate deploy` construiu um banco vazio do zero e o histórico foi reaplicado após recriação do banco de desenvolvimento. A materialização está documentada em [../engineering/database.md](../engineering/database.md), e [../engineering/environments.md](../engineering/environments.md) e `.env.example` passaram a registrar `DIRECT_URL` como a única variável consumida.

O que F1-002 **não** fez: nenhum Neon provisionado ou acessado, nenhuma credencial real, nenhuma variável em painel, nenhum banco `preview` ou `production`, nenhum `db push`, nenhuma Preview feature do Prisma, nenhum Prisma 8, nenhum singleton ou driver adapter de runtime, nenhum job de CI/CD de `migrate deploy`, nenhuma alteração no CI ou no ruleset, nenhum módulo de domínio, nenhuma integração, nenhuma funcionalidade de produto e nenhuma alteração de regra, requisito, ADR ou decisão. **E-5 permanece parcial** e a Fase 1 **não** foi declarada concluída.

### Conclusão de F1-003

F1-003 foi executado em **2026-09-14**, logo depois de F1-002, e é a **segunda parte do entregável E-5**: o runtime do Prisma Client. A pesquisa na documentação oficial do Prisma ORM 7 e do Neon confirmou que o Prisma 7 exige driver adapter em toda instância de `PrismaClient`, que o pool de conexões pertence ao driver, que a instância deve ser criada fora do handler e reutilizada sem `$disconnect()` por request, e que o runtime usa a URL pooled enquanto o CLI usa a direta. O adapter escolhido foi `@prisma/adapter-pg@7.10.0` sobre `pg@8.23.0`, ambos pinados: o Neon é provedor e não vocabulário da camada de persistência, as funções da Vercel rodam em Node.js com TCP disponível, e o guia do Neon que recomenda `@prisma/adapter-neon` (WebSockets) não apresenta razão técnica que se aplique a este projeto — a diferença ficou registrada em [../engineering/database.md](../engineering/database.md), seção 13.2, sem alterar ADR.

A entrega é a fronteira `src/persistence/prisma.ts` — nome da camada de persistência de AR-3.2, não um módulo de AR-3.3 —, que exporta apenas `getPrismaClient()` e o tipo `PrismaClient`: importa o client gerado exclusivamente de `src/generated/prisma`, lê `DATABASE_URL` de forma **lazy** na primeira chamada, nunca lê `DIRECT_URL`, guarda a instância única em `globalThis` para sobreviver ao hot reload, não desconecta por request e, sem a variável, lança erro que nomeia `DATABASE_URL` e nunca o valor. A prova foi feita em PostgreSQL 17.11 real e descartável em contêiner, migrado por `migrate deploy` a partir do banco vazio: `npm run test:integration`, suíte própria com `vitest.integration.config.mts`, fora de `test:ci`, executou conexão, `SELECT 1`, leitura de `_prisma_migrations` e `count()` no schema tipado, 5 de 5 casos; sem `DATABASE_URL` ela falha em vez de pular. Os testes unitários da fronteira entraram na suíte padrão. Um Client Component temporário que importava a fronteira fez `next build` falhar por módulos do Node ausentes no bundle de cliente, comprovando o caráter server-side; o arquivo foi removido. O contêiner e o volume foram destruídos ao final. `DATABASE_URL` passou a `consumido` em [../engineering/environments.md](../engineering/environments.md) e em `.env.example`.

O que F1-003 **não** fez: nenhum Neon provisionado ou acessado, nenhuma credencial real, nenhuma variável em painel ou na Vercel, nenhum job de CI/CD de `migrate deploy`, nenhuma alteração no CI ou no ruleset, nenhum repositório de domínio, nenhum módulo de AR-3.3, nenhum Route Handler ou Server Action, nenhuma integração, nenhuma alteração de `schema.prisma` ou nova migration, nenhum `db push`, nenhuma Preview feature, nenhum Prisma 8, nenhuma funcionalidade de produto e nenhuma alteração de regra, requisito, ADR ou decisão. **E-5 permanece parcial** e a Fase 1 **não** foi declarada concluída.

### Conclusão de F1-004

F1-004 foi executado em **2026-09-15**, logo depois de F1-003, e é a **terceira e última parte do entregável E-5**: o banco de `preview` e a aplicação controlada de migrations. A pesquisa na documentação oficial confirmou, antes de qualquer criação, que a região São Paulo (`aws-sa-east-1`) e o PostgreSQL 17 estão disponíveis na conta Neon, que nomes de GitHub Environment não distinguem maiúsculas e que secrets de environment só chegam a jobs que declaram o environment, que variável nova da Vercel só vale para deployments novos, e que `prisma migrate deploy` aplica as migrations pendentes, cria `_prisma_migrations` quando ausente, não reseta o banco, não procura drift e não gera artefato.

**Entregue:** projeto Neon `techlab-troq-preview` — a única organização Neon gratuita e não gerida pela integração da Vercel foi usada, pois não existia organização do TROQ e a API não cria organizações —, com PostgreSQL 17 escolhido explicitamente (o padrão da conta seria 18), branch inicial `preview` como branch padrão (o painel a rotula *production branch*, terminologia do provedor que não cria produção do TROQ), database `troq`, sem extensão, sem branching por PR e sem integração Neon–Vercel; as duas conexões geradas pelo provedor, verificadas com `SELECT 1`, `SHOW server_version` (17.11) e `current_database()` a partir do executor, sem aplicar migration e sem exibir a string; o GitHub Environment `preview` — o `Preview` preexistente, criado pelos registros de deployment da Vercel — com política de branch de deployment limitada a `main`, sem revisores e sem tempo de espera, e com os secrets `DIRECT_URL` e `DATABASE_URL`, gravados por entrada padrão e conferidos apenas por nome; nenhum secret de repositório; o workflow `.github/workflows/migrate-preview.yml` conforme o contrato de [../engineering/database.md](../engineering/database.md), seção 15.3; a documentação de engenharia (`database.md`, `environments.md`, `testing.md`, `.env.example`) e de estado sincronizadas. Os hashes de `prisma/schema.prisma` e da migration inicial foram conferidos idênticos antes e depois; nenhuma dependência foi alterada; o CI, o seu job e o ruleset de `main` foram preservados.

**Vercel:** o executor só obteve acesso ao projeto `techlab-troq` depois de Bruno autenticar o CLI da Vercel na máquina do executor (o token presente no ambiente pertencia a outro time e o conector Vercel não expõe operação de variáveis). Com essa credencial, por API e sem exibir valor, F1-004 criou `DATABASE_URL` (pooled, tipo sensível) e `APP_ENV=preview` **somente** no escopo Preview — o projeto não tinha nenhuma variável antes —, conferiu por nome que Production continua vazia e que `DIRECT_URL` não existe na Vercel, e disparou um redeploy do último preview da PR, concluído em `READY`.

**Primeira execução do workflow:** disparada pelo squash merge da PR #28 em `main` (commit `ae6e6f0`), run `34968277499`, job concluído em 1m14s com todos os passos `success`; `migrate deploy` aplicou `20260914210926_initial_schema`, `migrate status` respondeu que o schema está em dia e `test:integration` aprovou 5 de 5 casos contra o pooled. Conferência posterior, somente leitura: uma linha em `_prisma_migrations` com `finished_at` preenchido e sem rollback; 24 tabelas, 18 enums, 3 triggers; zero linhas de produto. A migration inicial é imutável desde então.

O que F1-004 **não** fez: nenhum recurso de `production`, nenhuma variável no escopo Production da Vercel, nenhum banco por PR ou branching efêmero do Neon, nenhum R2 ou Resend, nenhuma observabilidade, nenhum módulo de AR-3.3, nenhuma alteração de `schema.prisma`, de migration, de `package.json`, de `src/`, do CI existente ou do ruleset, nenhuma dependência nova, nenhum seed, nenhum required check novo, nenhuma funcionalidade de produto e nenhuma alteração de regra, requisito, ADR ou decisão. **E-5 está concluído** e a Fase 1 **não** foi declarada concluída: E-1 e E-6 seguem parciais, E-7 não iniciado, e o gate de saída não foi verificado.

### Conclusão de F1-005

F1-005 foi executado em **2026-09-15**, logo depois de F1-004, e **conclui o entregável E-1**. A auditoria de F0-023 registrava E-1 como `parcial` com uma única lacuna nomeada — a estrutura de módulos de AR-3.3 —, e o scaffold Next.js, o `tsconfig.json` em modo `strict`, o alias `@/*`, os comandos de qualidade e o CI já existiam e não foram tocados. Essa lacuna foi fechada.

**Entregue:** os onze diretórios de `src/modules/`, cada um com `index.ts` como ponto de entrada público — os nove módulos de domínio de AR-3.3 (`identity`, `contact`, `listing`, `media`, `request`, `payments`, `negotiation`, `reputation`, `moderation`), com os nomes literais da decisão, inclusive o plural de `payments`, e os dois transversais `audit` e `platform`. Cada `index.ts` documenta a responsabilidade do módulo e as suas entidades próprias conforme AR-3.3, cita as fronteiras que o justificam quando elas existem — AR-3.4 em `contact`, AR-3.5 em `request` e em `payments` — e declara-se como módulo TypeScript **sem comportamento**. **Nenhuma entidade, interface, serviço, repositório, DTO, comando, evento ou caso de uso foi criado**: a fronteira existe, a implementação não, e nada de produto foi antecipado. Não há `src/modules/index.ts`: um barrel global apagaria justamente a fronteira que [../engineering/conventions.md](../engineering/conventions.md), seção 2.2, exige. A convenção física ficou registrada na seção 2.5 daquele documento — diretório por módulo, `index.ts` como API pública, interior privado por padrão, import externo por `@/modules/<module>` e nunca por caminho interno, `audit` e `platform` como transversais, `src/app` como composição sem regra de negócio e `src/persistence` preservado no seu papel.

O que F1-005 **não** fez: nenhuma decisão arquitetural nova e nenhuma alteração de AR-3.3 — a decisão já existia e apenas foi materializada —, nenhuma dependência adicionada, removida ou atualizada, nenhuma alteração de `prisma/schema.prisma`, de migration ou de `prisma.config.ts`, nenhuma movimentação de `src/persistence/prisma.ts`, nenhuma alteração de `src/app`, do CI, dos scripts ou do ruleset, nenhum Route Handler, Server Action, UI, autenticação, upload, R2, Resend, pagamento, Mercado Pago, observabilidade, logging ou provisionamento externo. **E-6 continua parcial e E-7 continua não iniciado**, exatamente como estavam, e o gate de saída da Fase 1 **não** foi verificado.

**Próximo estado da Fase 1.** Restam E-6 (infraestrutura mínima: R2 e Resend não provisionados) e E-7 (observabilidade, não iniciada), além do banco de `production`, que nenhum item cobriu até aqui. Nenhum desses trabalhos foi iniciado por F1-005.

### Conclusão de F1-006

F1-006 foi executado em **2026-09-15**, logo depois de F1-005, e é a **primeira parte do entregável E-6**: o armazenamento de objetos. A pesquisa na documentação oficial da Cloudflare, feita antes de qualquer criação, confirmou que o R2 é cobrado **exclusivamente por uso**, sem mensalidade fixa, com franquia mensal de 10 GB-mês de armazenamento, 1 milhão de operações Classe A e 10 milhões Classe B, aplicável somente à classe `Standard`, e com egress gratuito; que o endpoint S3-compatible tem a forma `https://<ACCOUNT_ID>.r2.cloudflarestorage.com` e a região é `auto`; e que a permissão *Object Read & Write* de um token do R2 **aceita escopo por bucket**, o que torna viável o privilégio mínimo exigido por [../engineering/environments.md](../engineering/environments.md). A assinatura do R2 não existia na conta e foi ativada por Bruno: o checkout exigia meio de pagamento, que o executor não pode cadastrar, e a execução foi reportada como bloqueada até que isso fosse resolvido — o pedido em si é de `$0/mês`, sem cobrança fixa ou imediata.

**Entregue:** os dois buckets da conta `TechLab+`, `troq-media-development` e `troq-media-preview`, distintos e ambos em classe `Standard`, com a localização automática do provedor (Leste da América do Norte, ENAM) — registrada como fato, não como requisito de jurisdição. Os dois permanecem **privados**: o URL de desenvolvimento público (`r2.dev`) está desativado, não há Custom Domain, não há política de CORS e nenhum acesso anônimo foi habilitado. **Um bucket por ambiente**, e não um bucket para originais e outro para derivados: a separação entre a área temporária e a pública continua sendo lógica, dentro do bucket, como fixam AR-10.4 e DEC-028. Duas **Account API Tokens** foram criadas, `troq-media-development-rw` e `troq-media-preview-rw`, cada uma com permissão *Object Read & Write* restrita **apenas** ao bucket do seu ambiente; nenhuma credencial alcança todos os buckets da conta e nenhuma chave é compartilhada entre ambientes.

**Custódia.** As cinco variáveis do contrato — `R2_S3_ENDPOINT`, `R2_REGION`, `R2_BUCKET`, `R2_ACCESS_KEY_ID` e `R2_SECRET_ACCESS_KEY` — foram configuradas em `.env.local` na máquina de desenvolvimento, arquivo ignorado pelo Git e conferido mecanicamente como tal, e **somente** no escopo Preview do projeto `techlab-troq` da Vercel, com `R2_ACCESS_KEY_ID` e `R2_SECRET_ACCESS_KEY` marcadas como sensíveis. Conferido por nome e escopo que o Production da Vercel continua **sem nenhuma variável**. Nenhuma credencial foi adicionada ao GitHub Actions, porque nenhum workflow precisa delas. Nenhum segredo real foi versionado, exibido em documentação ou escrito em `.env.example`.

**Provas.** Com cada credencial, e por assinatura SigV4 feita em script descartável fora da árvore do projeto — sem SDK e sem dependência nova —, o ciclo completo passou nos dois ambientes: `HeadBucket` 200, `PutObject` 200, `GetObject` 200 com conteúdo idêntico ao gravado, `DeleteObject` 204 e nova leitura 404. `ListObjectsV2` sob o prefixo `_provisioning-check/` respondeu `KeyCount=0` nos dois buckets, comprovando que **nenhum objeto de teste permaneceu**. A prova negativa de isolamento foi executada nas quatro direções cruzadas — cada credencial contra o bucket do outro ambiente, em `HeadBucket` e `PutObject` — e todas retornaram **403**, sem que nenhuma permissão fosse ampliada para o teste. O scratch foi removido ao final.

O que F1-006 **não** fez: nenhum recurso de `production` e nenhuma variável no escopo Production, nenhum Resend, nenhuma integração do R2 no módulo `media`, nenhum AWS SDK ou dependência de aplicação, nenhuma presigned URL, nenhum upload, nenhum processamento de imagem, thumbnail ou derivado, nenhum `r2.dev`, Custom Domain, Worker, CDN ou domínio público de imagens, nenhuma política de CORS, nenhuma observabilidade, nenhuma alteração de `schema.prisma`, de migration, de `package.json`, de `package-lock.json`, de `src/`, do CI ou do ruleset, e nenhuma alteração de regra, requisito, ADR ou decisão. As cinco variáveis do R2 **continuam `previsto`** no contrato de ambientes, porque estar configurada em um painel não é o mesmo que ser lida por código versionado — e nenhum código as lê. **E-6 continua parcial**, agora exclusivamente pela ausência do Resend, **E-7 continua não iniciado**, e a Fase 1 **não** foi declarada concluída.

**Próximo estado da Fase 1.** Restam o Resend, que fecha E-6, e E-7 (observabilidade, não iniciada), além do banco de `production`, que nenhum item cobriu até aqui. Nenhum desses trabalhos foi iniciado por F1-006.

### Conclusão de F1-007

F1-007 foi executado em **2026-09-15**, logo depois de F1-006, e **conclui o entregável E-6**. A pesquisa na documentação oficial do Resend, feita antes de qualquer criação, fixou três fatos que determinaram o desenho: **é obrigatório verificar um domínio para enviar** — o endereço compartilhado `onboarding@resend.dev` é sandbox e só entrega ao email do dono da conta, logo não serve a remetente por ambiente; as **API keys aceitam permissão (*Sending access* ou *Full access*) e restrição por domínio**, e o seu valor é exibido **uma única vez**; e o plano gratuito passou a incluir **3 domínios verificados** (desde 25/08/2026), além de 3.000 emails por mês e 100 por dia, o que acomoda os dois ambientes e ainda deixa folga para `production`. O provedor também oferece endereços **simulados** (`delivered@`, `bounced@`, `complained@`, `suppressed@resend.dev`), que permitem provar envio sem tocar em pessoa real.

**Entregue:** dois **subdomínios remetentes verificados** sob o domínio `troqs.app`, que o TROQ já possuía na Cloudflare — `dev.troqs.app` para `development` e `preview.troqs.app` para `preview` —, ambos na região `sa-east-1` (São Paulo), que era o padrão oferecido pelo provedor e não uma decisão de jurisdição criada por esta tarefa. Subdomínio por ambiente, e não o domínio raiz: isso isola a reputação de envio de cada ambiente e é o que permite restringir cada credencial ao seu próprio remetente. Os seis registros DNS exigidos — DKIM (`TXT`), SPF (`TXT`) e `MX`, três por subdomínio — foram criados **manualmente** na zona `troqs.app`, que estava vazia. A opção *Auto configure* do Resend foi recusada deliberadamente: ela exige conceder ao provedor acesso OAuth de escrita ao DNS da conta Cloudflare, privilégio mais amplo do que a tarefa precisa e autorização que não cabia ao executor conceder. Duas **API keys** foram criadas, `troq-development-sending` e `troq-preview-sending`, ambas com permissão **Sending access** — nunca *Full access* — e **restritas ao domínio do seu ambiente**.

**Custódia.** `RESEND_API_KEY` e `EMAIL_FROM` foram configuradas em `.env.local` na máquina de desenvolvimento, arquivo ignorado pelo Git e conferido mecanicamente como tal, e **somente** no escopo Preview do projeto `techlab-troq` da Vercel, com `RESEND_API_KEY` marcada como sensível. Conferido por nome e escopo que o Production da Vercel continua **sem nenhuma variável**. Nenhum segredo real foi versionado, exibido em documentação ou escrito em `.env.example`.

**Provas.** Os dois subdomínios foram conferidos por resolução DNS pública real antes da verificação, e os seis registros responderam com o conteúdo exato publicado. O Resend marcou **`Verified`** os dois. Com cada chave, e por script descartável fora da árvore do projeto — sem SDK e sem dependência nova —, um envio real partiu do remetente do seu ambiente para o endereço **simulado** `delivered@resend.dev`, **nunca uma pessoa real**, como exige a seção 5.4 de [../engineering/environments.md](../engineering/environments.md): ambos retornaram **HTTP 200** com identificador de mensagem. A prova negativa de isolamento foi executada nas duas direções cruzadas — cada chave tentando enviar pelo domínio do outro ambiente — e ambas retornaram **HTTP 403**, com o provedor declarando explicitamente que a chave não está autorizada para aquele domínio. O scratch foi removido ao final.

O que F1-007 **não** fez: nenhum recurso ou variável de `production`, nenhum domínio de produção, nenhuma integração de email em código, nenhum SDK ou dependência de aplicação, nenhum template, layout ou conteúdo de mensagem, nenhum webhook, nenhuma automação, audiência ou broadcast, nenhum registro DMARC — que é opcional no provedor e seria política de zona mais ampla que esta tarefa —, nenhuma observabilidade, nenhuma alteração de `schema.prisma`, de migration, de `package.json`, de `package-lock.json`, de `src/`, do CI ou do ruleset, e nenhuma alteração de regra, requisito, ADR ou decisão. `RESEND_API_KEY` e `EMAIL_FROM` **continuam `previsto`** no contrato de ambientes, porque estar configurada em um painel não é ser lida por código versionado — e nenhum código as lê. **E-6 está concluído**; **E-7 continua não iniciado**; `production` continua inexistente; e a Fase 1 **não** foi declarada concluída.

**Próximo estado da Fase 1.** Resta **E-7** (observabilidade, não iniciada) e o banco de `production`, que nenhum item cobriu até aqui. Nenhum desses trabalhos foi iniciado por F1-007.

### Conclusão de F1-008

F1-008 foi executado em **2026-09-15**, logo depois de F1-007. Ele é **decisão arquitetural e contrato**, e é o primeiro trabalho a tocar **E-7** — sem concluí-lo. A lacuna que fechou existia desde a Fase 0: [../architecture/overview.md](../architecture/overview.md) (AR-14.1) exigia logs estruturados e rastreamento de erros mas atribuía expressamente à Fase 1 a escolha da **ferramenta concreta e do escopo**, e por isso **RNF-018** estava `parcialmente definido` e [../engineering/environments.md](../engineering/environments.md) registrava a observabilidade como área **sem nenhuma variável**.

**Pesquisa.** A comparação foi feita em 2026-09-15 contra documentação oficial dos três candidatos, e dois fatos decidiram. Primeiro, **a observabilidade nativa da Vercel não atende** ao que AR-14.1 pede: os eventos rastreados são de plataforma — Edge Requests, invocações de Function, Middleware, APIs externas —, de modo que um erro não tratado no navegador não produz evento; e a retenção incluída é de **12 horas no Hobby e 1 dia no Pro**, com 30 dias somente na Observability Plus, que exige Paid Pro e é cobrada por evento. **Drains não resolvem isso:** são transporte para um destino externo, cobrados por GB, indisponíveis no Hobby — adotá-los pressupõe escolher outra ferramenta em vez de substituir a escolha. Segundo, **o Better Stack colide com o contrato de ambientes**: o cliente oficial de Next.js exige o *source token* em `NEXT_PUBLIC_BETTER_STACK_SOURCE_TOKEN`, variável **pública** por construção, o que a seção 3.2 daquele contrato proíbe sem exceção; a documentação do cliente também não trata de source maps nem de *scrubbing* de PII, e a retenção gratuita é de **3 dias**. O **Sentry** reúne, no mesmo produto e na franquia gratuita, error monitoring de cliente e de servidor com guia oficial de App Router, logs estruturados, tracing, source maps, **30 dias de retenção** para erros, spans e logs, *data scrubbing* server-side **habilitado por padrão** (que já remove campos com `token`, `auth`, `credentials`, `secret` e `bearer` no nome), `sendDefaultPii` com padrão `false` e **projetos ilimitados** em todos os planos. Os onze fatos apurados, com fonte, estão na seção *Pesquisa externa* de [../adr/0007-observability-sentry.md](../adr/0007-observability-sentry.md).

**Entregue:** [../adr/0007-observability-sentry.md](../adr/0007-observability-sentry.md), que registra **DEC-039** e decide, em quinze itens: Sentry SaaS como plataforma de observabilidade do MVP; **um projeto e um DSN por ambiente**, sem compartilhamento entre `development`, `preview` e `production` — divergência **deliberada e registrada** da orientação do próprio provedor, que recomenda um projeto por codebase com separação por tag, porque a norma de isolamento do TROQ é mais forte e a seção 6.1, regra 3, proíbe reaproveitar segredo entre ambientes; o escopo funcional da instrumentação futura, incluindo tornar os **seis sinais de AR-14.3 operacionalmente observáveis, sem reduzi-los, reinterpretá-los nem substituí-los**; que **telemetria nunca é estado autoritativo**, permanecendo no banco e nos módulos de domínio o estado financeiro, a auditoria, a autorização e a capacidade; uma **lista explícita e conferível** de dados proibidos na telemetria — telefone, WhatsApp, email quando não estritamente necessário, nome, CPF, endereço, tokens, cookies de sessão, cabeçalhos `Authorization`, credenciais, secrets, strings de conexão, request bodies arbitrários, respostas integrais de provedores, payloads integrais de webhooks e dados financeiros sensíveis; que telefone/WhatsApp **nunca** é tag, atributo, contexto, breadcrumb, mensagem nem chave de correlação, em nenhuma forma derivada, truncada, mascarada ou com hash; a preservação ou o reforço do *scrubbing* do provedor, com proibição de habilitar `sendDefaultPii` ou a opção que a suceder por conveniência; **Session Replay fora do MVP e desabilitado**, cuja habilitação futura exige nova análise explícita de privacidade e necessidade; a reafirmação integral de **AR-14.2** — uma operação crítica nunca é auditada por aparecer no Sentry, e a trilha de auditoria continua sendo estado versionado no PostgreSQL; e o registro da dependência externa e da franquia a acompanhar sob **R-08**, sem criar compromisso comercial.

**Contrato de variáveis.** A seção 5.8 de [../engineering/environments.md](../engineering/environments.md) deixou de ser "área sem variável" e passou a conter quatro entradas, todas em estado **`previsto`**: `NEXT_PUBLIC_SENTRY_DSN` (os três ambientes, **pública**, **não** é segredo, obrigatória quando a instrumentação estiver ativa, um valor por ambiente); `SENTRY_AUTH_TOKEN` (server-side, **segredo**, **condicional** — só onde o build/CI realmente autenticar no provedor, nunca acessível ao browser, nunca lida em runtime, menor escopo e credencial por ambiente); `SENTRY_ORG` e `SENTRY_PROJECT` (server-side, **não** segredos, configuração de build, condicionais à integração de source maps/releases, com `SENTRY_PROJECT` distinta por ambiente). Os quatro nomes foram conferidos como **convenção documentada** da ferramenta, conforme a regra 4 da seção 4. Ficou registrado que a DSN é **chave de ingestão endereçada** — não lê issues, não lê logs, não administra projeto, não emite credencial — e que ser pública **não** autoriza expor nenhuma outra credencial. **Nenhuma variável especulativa foi criada:** não existem `SENTRY_ENVIRONMENT` nem `NEXT_PUBLIC_SENTRY_ENVIRONMENT`, porque o ambiente já é `APP_ENV` e o guia de JavaScript do provedor não declara variável de ambiente para essa opção; não existe variável própria de release; e **não** foi criada uma segunda `SENTRY_DSN` duplicando o mesmo valor — a instrumentação server-side usará explicitamente a mesma DSN, e duas variáveis com o mesmo conteúdo só criariam duas fontes de verdade e um modo de falha. `.env.example` recebeu a área 8 correspondente, na mesma ordem do catálogo, apenas com placeholders obviamente fictícios.

**Requisito.** Com ferramenta e escopo inequivocamente definidos, **RNF-018 passou de `parcialmente definido` a `definido`** em [../product/requirements.md](../product/requirements.md), e a contagem dos não funcionais foi ajustada de 11/7 para 12/6. Isso é **definição do requisito, não implementação**: o requisito não foi declarado implementado, validado em runtime nem concluído operacionalmente. **AR-14.1** deixou de dizer que a ferramenta ainda seria escolhida e passou a referenciar ADR-0007, preservando-se integralmente **AR-14.2**, **AR-14.3** — com os seis sinais mínimos intactos — e **AR-14.4**.

O que F1-008 **não** fez: nenhuma conta, organização ou projeto Sentry criado ou acessado, nenhum painel do provedor tocado, nenhum DSN real, nenhum token real, nenhuma variável configurada em painel ou na Vercel, nenhuma alteração de `.env.local`, nenhuma dependência instalada — `@sentry/nextjs` **não** consta de `package.json` —, nenhum wizard executado, nenhuma alteração de `next.config.*`, nenhum arquivo de instrumentação, nenhum logger, nenhum log ou trace emitido, nenhum dashboard, nenhum alerta, nenhuma configuração de source maps, nenhum Session Replay, nenhuma integração Vercel–Sentry, nenhum recurso de `production`, nenhuma alteração de `src/`, de `prisma/`, de `schema.prisma`, de migration, do CI ou do ruleset, e nenhuma alteração de regra de negócio, de ADR anterior ou de decisão de produto. As quatro variáveis **continuam `previsto`**, porque nenhum código versionado as lê. **E-7 continua `não iniciado`** — decisão documental não é telemetria — e a Fase 1 **não** foi declarada concluída.

**Próximo estado da Fase 1.** O próximo item é **F1-009 — provisionamento do Sentry em `development` e `preview`**, que criará os projetos reais, configurará a privacidade no provedor, obterá DSN e credenciais e custodiará os valores nos ambientes. A **instrumentação do SDK** permanece trabalho posterior e próprio, e receberá o seu próprio identificador quando F1-009 concluir: decisão, provisionamento e instrumentação são três trabalhos distintos, nesta ordem, e não devem ser misturados ([../adr/0007-observability-sentry.md](../adr/0007-observability-sentry.md), decisão 15). Resta também o banco de `production`, que nenhum item cobriu até aqui.

### Conclusão de F1-009

F1-009 foi executado em **2026-09-15**, logo depois de F1-008. Ele é **provisionamento**, o segundo dos três trabalhos que [../adr/0007-observability-sentry.md](../adr/0007-observability-sentry.md), decisão 15, separa deliberadamente — decisão, provisionamento e instrumentação —, e **não** conclui **E-7**.

**Pesquisa.** A documentação oficial do provedor foi reconferida em 2026-09-15, antes de configurar qualquer coisa, e **nenhum fato encontrado contradiz a ADR-0007**: o *data scrubbing* server-side continua **habilitado por padrão**, com os mesmos scrubbers de nome de campo que S-7 registrou, configurável por projeto e por organização, com *Additional Sensitive Fields*, *Safe Fields* e opção de não armazenar endereço IP; cada projeto continua tendo **DSN própria**, contendo o seu próprio identificador de projeto, e a DSN continua sendo segura como pública por só permitir ingestão; o plano **Developer** continua gratuito com 5 mil erros, 5 milhões de spans, 5 GB de logs, 50 replays e um único usuário. Na plataforma de deploy, confirmou-se que uma variável pode ser escopada **somente a Preview** e que mudanças de variável **não** afetam deployments anteriores — razão pela qual **nenhum redeploy foi disparado**, já que nenhum código ainda lê a variável. Nada disso justifica reabrir a ADR.

**Organização e plano.** A organização Sentry `techlab-bt` ("TechLab+") já existia, sob a conta autenticada e inequivocamente destinada ao TechLab+/TROQ, e foi **reutilizada** — nenhuma organização nova foi criada. Ela contém **apenas** projetos do TROQ. A organização opera em **trial** do plano Business até **2026-09-29**, **sem meio de pagamento registrado** e com gasto sob demanda zerado; o provedor documenta que, ao fim do trial, uma organização vinda do plano Developer **retorna ao Developer gratuito**, sem troca automática para plano pago. **Nenhum compromisso financeiro foi criado, nenhum upgrade foi aceito e nenhum cartão foi informado.**

**Entregue:** dois projetos Sentry reais, **um por ambiente** e com plataforma Next.js selecionada — `techlab-troq-development` e `techlab-troq-preview` —, com identificadores de projeto distintos e **client keys distintas**, cada DSN terminada no identificador do seu próprio projeto. O isolamento foi comprovado **estruturalmente** — projetos, keys e DSNs distintos — e **funcionalmente**: um evento sintético de prova, com dados fictícios e neutros e explicitamente rotulado como probe de F1-009, foi enviado a cada projeto pelo endpoint de ingestão, **sem instalar SDK e sem alterar o repositório**, e cada evento apareceu **exclusivamente** no projeto correspondente. As duas issues de prova foram **resolvidas** em seguida. Nenhum dado real, pessoal, financeiro ou de negócio foi usado.

**Privacidade configurada no provedor.** Nos dois projetos e também no nível da organização: *data scrubbing* server-side **habilitado**, **scrubbers padrão preservados** — nada foi removido nem substituído —, campos sensíveis do domínio **acrescentados** (`phone`, `telefone`, `whatsapp`, `email`, `cpf`, `address`, `endereco`), **nenhum *safe field*** declarado e armazenamento de **endereço IP desabilitado**, que é a configuração mais restritiva disponível. O reforço no nível organizacional foi seguro porque a organização não contém projeto de terceiro. **Session Replay permanece desabilitado** e `sendDefaultPii` **não** foi habilitada — sem SDK, ambas seguem no padrão do provedor, que é exatamente o estado que as decisões 7 e 9 exigem.

**Custódia.** `NEXT_PUBLIC_SENTRY_DSN` de `development` vive **apenas** em `.env.local`, arquivo local ignorado pelo versionamento, onde as configurações preexistentes foram integralmente preservadas e somente a variável nova foi acrescentada; a de `preview` vive **apenas** no escopo **Preview** do projeto na plataforma de deploy, classificada como variável comum e **não** como segredo, porque a DSN é pública por contrato. **Nenhuma variável do Sentry foi configurada em Production**, e nenhum valor real trafegou por commit, PR, documentação ou relatório.

O que F1-009 **não** fez: nenhum recurso, variável ou projeto de `production`; **nenhum `SENTRY_AUTH_TOKEN` criado** — a organização tem zero tokens de autenticação —; `SENTRY_ORG` e `SENTRY_PROJECT` **não** configuradas como variáveis de ambiente, por serem condicionais a source maps e releases, que **não** foram adotados; nenhum source map, release, integração de CI ou execução de `sentry-cli`; nenhum wizard do provedor executado; `@sentry/nextjs` **não** instalado e nenhuma dependência adicionada; nenhum `instrumentation.ts`, `instrumentation-client.ts`, `sentry.server.config.*`, `sentry.edge.config.*` ou `global-error.tsx`; nenhuma alteração de `next.config.*`, de `src/`, de `prisma/`, de `schema.prisma`, de migration, de `package.json`, de `package-lock.json`, do CI ou do ruleset; nenhum dashboard, alerta, integração Vercel–Sentry ou integração GitHub–Sentry; nenhuma telemetria da aplicação instrumentada; e nenhuma alteração de regra de negócio, de requisito, de ADR anterior ou de decisão de produto. As quatro variáveis da seção 5.8 **continuam `previsto`**: um valor real passar a existir não cria consumidor versionado, e nenhum código as lê. **E-7 continua `não iniciado`** — provisionar infraestrutura não produz observabilidade funcional da aplicação — e a Fase 1 **não** foi declarada concluída.

**Próximo estado da Fase 1.** O próximo item é **F1-010 — instrumentação do Sentry em `development` e `preview`**, que instalará a SDK, integrará ao App Router e validará a emissão contra os projetos isolados que agora existem. Resta também o banco de `production`, que nenhum item cobriu até aqui.

## Fora deste backlog

- Tarefas de implementação da Fase 2 em diante (telas, fluxos, integrações de produto): detalhadas no backlog da própria fase, não aqui. O scaffold e o CI que o roadmap lista como entregáveis da Fase 1 **já existem** e não devem ser recriados ([phase-1-transition.md](phase-1-transition.md), seção 10).
- Candidatos pós-MVP listados em [roadmap.md](roadmap.md).

## Revisão

Atualizar este documento a cada item concluído, sempre na mesma PR que registra o resultado, e ao fechar cada decisão aberta.
